# Configuration Files

This directory contains environment-specific configuration files:

- `config.dev.yaml` - Development environment configuration
- `config.prod.yaml` - Production environment configuration  
- `config.test.yaml` - Testing environment configuration

These files supplement the main `.env` file and provide structured configuration for different deployment environments.